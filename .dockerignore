# 构建上下文瘦身：这些文件不需要进镜像。
# 同时它们变动频繁，排除后可避免无谓地让最后的 COPY . . 层缓存失效。

# Python 字节码缓存
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 虚拟环境
.venv/
venv/
env/

# 版本控制与编辑器
.git/
.gitignore
.vscode/
.idea/

# 截图等文档资源（README 用，容器运行不需要）
*.png

# 本地数据库文件（运行时应挂载或连接外部 MongoDB，不打包进镜像）
*.db
*.sqlite
*.sqlite3

# Node.js 侧与本项目无关的脚手架 / 实验文件
node_modules/
npm-debug.log*

# 注意：不要把 Dockerfile / .dockerignore 写进本文件。
# 忽略 Dockerfile 会让构建上下文缺少构建说明文件，docker build 会直接失败。
